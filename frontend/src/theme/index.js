import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#D5AD56",
    },
    secondary: {
      main: "#dc004e",
    },
    mode: "dark",
  },
  typography: {
    fontFamily: "Montserrat, sans-serif",
  },
});
